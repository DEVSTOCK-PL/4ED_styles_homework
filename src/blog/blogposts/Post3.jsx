import BlogPost from '../BlogPost'

const Post3 = () => {
  const header = 'Spotifys Car Thing available to all premium users'
  const blurb =
    "Starting Tuesday any Spotify Premium user can buy the streaming service's first piece of hardware."
  return <BlogPost header={header} blurb={blurb} />
}

export default Post3
